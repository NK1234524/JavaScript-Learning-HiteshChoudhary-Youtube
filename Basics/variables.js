const accountId=14453;
let accountEmail="nishant@company.com";
var accountPassword ="1223";
accountcity = "Jaipur";

let accountstate;

//In JS earlier Js work on the basis of scope so to include scope " " so for that we include var.
//console.log(accountId=122); Assignment to const value not allowed

console.table([accountEmail,accountId,accountPassword,accountPassword,accountstate]);
